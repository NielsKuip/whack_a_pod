apiVersion: apps/v1
kind: Deployment
metadata:
  name: game-deployment
spec:
  replicas: 12
  selector:
    matchLabels:
      app: game
  template:
    metadata:
      labels:
        app: game
    spec:
      containers:
      - name: game-deployment
        image: docker pull gcr.io/kube-224111/game:latest
        ports:
        - containerPort: 8080
